from to --> bhopla to guna
// no errors all constraint are handeled in front end like adding string to interger 
or guna to bhopal --> any route
date == 1/12/2022 -- you can add other dates too

if mobile no exceeds 10 shows pop error
if you input string in integer database they will pop up invalid input

also you can open the database in visual studio code using sqlite viewer extension 